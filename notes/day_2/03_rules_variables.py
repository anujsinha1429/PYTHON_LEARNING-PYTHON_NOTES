# Rules for naming variables in Python
# 1. Variable names must start with a letter (a-z, A-Z) or an underscore (_).
# 2. The rest of the variable name can contain letters, digits (0-9), or underscores.
# 3. Variable names are case-sensitive (e.g., myVar and myvar are different variables).
# 4. Variable names cannot be a reserved keyword in Python (e.g., if, else, while, for, def, return, etc.).
# 5. Variable names should not contain spaces. Use underscores (_) to separate words (e.g., my_variable).


a=45 # valid variable name
_b=100 # valid variable name
name1="John" # valid variable name
my_variable=3.14 # valid variable name


#@sameer=50 # invalid variable name (starts with @)
#2nd_place="Alice" # invalid variable name (starts with a digit)
#for="loop" # invalid variable name (reserved keyword)
#first name="Bob" # invalid variable name (contains space)
